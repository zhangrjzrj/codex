# 回答风格与协作规则（长期个人偏好，精简版）

## 0. 适用范围与实验性方法
- 以下规则作为用户级全局偏好，默认对所有会话生效。
- 高风险操作或需要门禁闭环时，优先启用 `legion-dev`。
- 实验性方法论（机制防僵化、逆向/正向二分互斥、稳>快执行细则）不作为全局硬约束；仅在显式启用 `legion-dev`（或等价实验技能）时生效。

## 1. 输出风格总则（结论优先）
- 先结论，后展开；第一段给“一针见血”的结论。
- 优先使用大白话与业务语言；术语需一句话解释。
- 解释代码时优先给关键代码片段与关键路径；不要平均分配篇幅到次要细节。
- 文件路径可给，行号仅在用户明确要求或确实有助于精确修改时提供。
- 复杂逻辑优先给 ASCII 流程图，先总图后关键节点。
- 推荐输出顺序：结论 -> 原因 -> 方案/改动 -> 验证结果 -> 下一步。
- 用户要求简短时，仅保留“结论 + 关键动作”。
- 简洁优先：默认追求必要且充分；能用一个清晰方案解决的问题，不堆多个方案；不省略关键风险和验证结果。

## 2. 操作边界（只读默认继续，业务写入先确认）
- 默认只在工作区（changes）产生修改。
- 未经用户明确指示，不得执行会影响暂存区（staged）的操作。
- 禁止主动执行：`git add`、`git restore --staged`、`git reset`（含影响暂存区形式）等。
- 只读排查（读代码、搜索、调用链分析）默认自动继续，不需单独询问。
- 涉及改代码、改业务配置、应用 patch 等会影响项目行为的操作，先明确告知并等待用户确认。
- `.codex-memory/` 下的会话记忆落盘属于已授权的项目内记录，可在每轮结束前自动执行；除非用户明确要求本轮不要写记忆。

## 2.1 代码注释规范
- 注释中不要带任何人名。

## 2.2 Git 提交规范
- git commit message 和提交 comment 默认使用中文，除非用户明确要求英文。
- 跨工作区 cherry-pick 提交时，默认保持源提交的 commit title 不变，便于通过标题追溯源头与主仓库合入关系；只有在合并多个提交、解决冲突导致语义变化，或用户明确要求时，才允许调整标题。

## 2.3 Codex 技能修改校验
- 新增或修改 Codex skill 后，必须在提交或推送前做格式校验。
- `SKILL.md` frontmatter 的 `name`、`description` 优先使用显式字符串引号，避免 YAML 解析歧义。
- 必须运行 `quick_validate.py <skill-dir>`；如果包含脚本，还要做语言级解析检查：
  - PowerShell：`[scriptblock]::Create((Get-Content -Raw <script>))`
  - Python：`python -m py_compile <script>`
  - JSON/YAML：用对应解析器读取一次
- 校验未通过时，不允许提交或推送该 skill。

## 2.4 本地生成物与 changes 区清爽规则
- Codex 生成的临时截图、UI dump、日志、诊断快照、一次性测试产物，应优先放入当前项目已被 `.gitignore` 忽略的本地证据/临时目录。
- 生成文件前，应优先查看项目是否已有被忽略的本地目录约定，例如本地 artifacts、evidence、tmp、diagnostics 目录；不要默认散落到项目根目录。
- 不得把代码、业务配置、脚本、正式文档、用户要求审阅的文件、需要提交/pick/push 的文件放入 ignored 目录。
- changes 区应尽量只保留用户需要判断、提交、合并、pick 或同步的项目文件。

## 2.4.1 图片与截图 payload 控制
- 大图片/截图进入模型前，默认优先生成轻量预览图以降低 payload；原图保留在本地证据目录。
- 若截图来自带坐标映射的自动化通道（如 CDP MCP、ADB/UIA 点击定位等），不得擅自二次压缩用于坐标反算；应优先使用该通道自身的截图、压缩比例和坐标元数据。

## 2.5 根因优先与临时止血约束
- 默认禁止“补丁摞补丁”：遇到 bug、构建失败、测试失败、状态异常时，应优先定位根因、调用链和数据流，不得只通过表层 if、延时、重试、吞异常、硬编码兜底来掩盖问题。
- 默认禁止在任何语言、任何代码中新增异常捕获或吞错逻辑，包括但不限于 JavaScript/TypeScript 的 `try/catch`、PHP 的 `try/catch`、C++ 的 `try/catch`、Python 的 `try/except`、Shell/PowerShell 的静默 `catch`、吞错重定向或忽略返回值；确需使用前必须先征得用户明确同意。
- 默认禁止新增止血/兜底代码；任何语言、任何位置如需新增临时止血、兜底、重试、延时、吞错、默认值回退、硬编码特殊分支、失败后继续执行等逻辑，必须先征得用户明确同意。
- 申请新增止血/兜底代码前，必须说明：根因是否已确认、为什么当前不能直接修根因、覆盖的是合法边界条件还是临时规避异常、风险范围、回收条件、对应测试如何证明没有掩盖问题。
- 经用户同意后添加的临时止血必须显式标注为 `WORKAROUND:`，并在代码或配置附近写清楚：为什么临时绕过、根因状态、风险范围、回收条件。
- 如果使用兜底逻辑，必须区分它覆盖的是合法边界条件，还是临时规避异常；前者可以保留，后者必须进入待清理项。
- 反馈闭环中，每轮修改前应先判断：这个改动是在修根因，还是在绕现象；如果是绕现象，必须说明为什么当前不能直接修根因。
- 禁止无标注的 sleep、重试、吞异常、硬编码兜底长期存在；后续可用 `rg "WORKAROUND|Root cause pending|Remove when"` 搜索回收。

## 3. 反馈闭环模式
- 当用户明确要求“反馈闭环/闭环直到成功/自循环直到成功”等，进入反馈闭环。
- 默认流程：测试/执行 -> 收集日志和结果 -> 分析根因 -> 修改代码或调整方案 -> 编译（如需）-> 再测试/再执行 -> 继续循环。
- 除非遇到真实阻塞（权限不足、环境缺失、外部依赖不可用、目标定义不清），不要停在“只分析不执行”。
- 每轮必须给出：
  1) 本轮目标
  2) 本轮证据
  3) 本轮动作
  4) 是否进入下一轮
- 停止条件仅有：
  - 用户目标已达到
  - 证据充分证明当前路径无法继续并明确卡点
  - 用户主动中止
- Messiah 场景优先复用专项技能（测试编排/Telnet/编译修复/RenderDoc/EXR 等），避免临时拼接流程。

## 3.1 Windows GUI 进程周期检查与熔断
- Windows GUI 反馈闭环中，不得因调试对象反复弹出模态窗口、无响应或卡死而无限等待。
- 启动游戏、编辑器、RenderDoc、VS 调试对象等长时间 GUI 进程时，优先由 Codex 后台启动进程并记录 PID，避免同步阻塞等待。
- 进程运行期间，Codex 应每隔一段时间主动检查进程状态、窗口状态、最近日志和必要的超时条件。
- 如果发现明确致命弹窗、进程无响应、日志长期不推进或超过本轮超时，应记录 PID、进程路径、窗口标题/正文、最近日志后终止目标进程。
- 熔断后，Codex 应基于本轮收集的证据继续分析，不应停在“等待用户手动关窗”。

## 4. 会话记忆落盘（项目内）
- 每轮结束前落盘“当前进度 + 关键结论 + 下一步建议”。
- 记忆路径固定：`.codex-memory/`（不存在可自动创建）。
- 默认结构：
  - `.codex-memory/index.md`
  - `.codex-memory/threads/*.md`
- 记录要求：简洁、可执行、便于续做；优先记录目标、已完成动作、关键结论、关键产物路径、未完成事项、下一步。
- `index.md` 做主题索引；具体内容写入对应 thread，避免长期堆到单一 `worklog.md`。
- 禁止跨工作目录混写会话记忆。

## 5. 记忆恢复与主题绑定
- 用户要求“恢复记忆/找回 xxx 结论”时：先读 `.codex-memory/index.md`，再读相关 `threads/*.md`，提炼最近且最相关结论后继续执行。
- 当前窗口稳定主题名优先级：
  1) 用户 `/rename` 新名称
  2) 用户显式声明主题名
  3) Codex 按当前主线推断
- 用户显式改主题时：仅允许重命名当前窗口绑定 thread，并同步更新 `index.md` 对应项；禁止批量改动其他主题。
- 主题改名动作：重命名当前绑定 thread -> 更新 `index.md` 对应索引 -> 后续继续写入新文件。

---

## 附录：原规则映射
- 原 0 -> 新 0
- 原 1/2/3/4/5 -> 新 1
- 原 6/7 -> 新 2
- 原 8 -> 新 3
- 原 9 -> 新 4
- 原 10/11 -> 新 5
- 原 12（方法论隔离）-> 新 0（并入）
<!-- EOF -->
