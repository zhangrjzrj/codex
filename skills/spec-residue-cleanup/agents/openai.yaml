interface:
  display_name: "清除过程残留"
  short_description: "清除 Spec 中已废弃方案、旧术语和讨论过程残留"
  default_prompt: "Use $spec-residue-cleanup to audit and remove process residue while preserving current rules and necessary gates."
